VERDICT: **AMEND** — the DELETE stands; amend it with a landing-order and excision manifest, because ruling #35 as written is not independently landable.

**Exact amendment:** *"#35 lands in the same commit as the Drive deletion (#33/#34) — `shadow-drive.py` imports `shadow_roster_lib`/`shadow_route_lib` at module load (lines 33–34), so deleting the libs alone breaks `shadow drive` at import. The same commit must: (a) remove the `roster`/`seat`/`route` dispatch entries from `bin/shadow` (lines 107, 127, and the route block); (b) drop the three libs + three CLIs from `shadow-release-package.py`'s manifest or packaging breaks; (c) excise from `scripts/shadow-host.py`: imports (28–30), `route_file_path` (730–748), `route_reference` (752–801), the `--route-file`/`--roster-file`/`--use-seat`/`--seat-file` flags and guards (840–859, 977–978), `selector_flag` + selector threading in host command construction (~310, 340–387, 848–859), and `payload["route"]` (954–955). The `_private_selector_present` receipt scrub becomes dead code and may die with the selectors. No behavior change to bare `shadow host run --host <x>` — that is already the shipped roster-free path."*

**Three strongest points:**

1. **The orphan premise is false in shipped code — I tried it as the overturn vector and it dies on the source.** `--route-file` is declared *optional* (`shadow-host.py:977`), `route_reference()` returns `None` when absent (757–758), and `run_attempt` proceeds to launch. The only hard dependency is `--use-seat`, which requires a route (843–844) — a flag inside the subsystem being deleted. The entire sealed path — frozen-task sha256 (836), clean-worktree gate (867), allowed-path sealing (868–879), receipt scrubbing — is roster-independent. The minimal delegation story is not hypothetical; it is the current default: `shadow host run --host codex --task-file … --allowed-path … --out …`. **Zero new code is needed for roster-free host runs.**

2. **The rent argument survives Leo's own steer tonight.** He asked to "delegate to Sol and fan out research to cheaper models like cursor" — a provider/cost/account question. roster.md's own text disclaims exactly that ("does not choose a provider or model, measure account quota…", quoted in r2-ruling-03 §3); provider/account choice already lives in `~/.config/leo/routing.json` (r1-seat-05 C) and the Sol doctrine. A router that structurally cannot express the only routing dimension its user routes on — and that mis-handles `ui` tasks (r1-seat-01 Q1) and non-code work in the #1 entity (r1-seat-08 B) — is a second router in a product whose preamble bans second routers. Confirmed, not overturned.

3. **What is genuinely lost is one narrow capability, and it has a named replacement.** Seat's only runtime effect is appending one validated single-token account/profile selector to the native argv while scrubbing it from receipts (`selector_for_route`, `shadow_seat_lib.py:325–339`; `selector_flag`, host.py:310). Post-delete, account pinning for a sealed run happens outside Shadow (delegate/routing.json launch context or env, e.g. `CODEX_HOME`) — consistent with the standing no-OAuth-pooling law. One drafting nit for the record: r2-ruling-03's surviving demotion sentence anchors on the CLAIM `seat=` token, which ruling #8 deletes; the chief's AGENT.md v2 correctly carries neither, so the demotion should read simply "provider, model, and account selection live outside Shadow; `shadow host run` takes `--host <name>` directly."

**The single Leo-decision (his call):** Does Shadow keep any account-pinning surface at all? **A (default): No** — `--host <name>` only; account/profile choice lives permanently in delegate/routing.json/env, per tonight's steer and the no-pooling law. **B:** retain one dumb `--profile <token>` single-token passthrough (~15 lines incl. receipt scrub) for account-pinned sealed runs, deleting the other ~1,760. Default A; flip to B only on the first real incident where a sealed run needed an account pin that env-level config could not express.